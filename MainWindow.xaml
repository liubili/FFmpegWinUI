<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FFmpegWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FFmpegWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FFmpegWinUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 全局 InfoBar 区域 -->
        <StackPanel Grid.Row="0" x:Name="InfoBarPanel" />

        <!-- 主导航区域 -->
        <NavigationView Grid.Row="1" x:Name="nvSample">
            <NavigationView.MenuItems>
                <!-- 起始页 -->
                <NavigationViewItem Icon="Home" Content="3FUI" Tag="HomePage" />
                <!-- 编码队列 -->
                <NavigationViewItem Content="编码队列" Tag="QueuePage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8FD;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 准备文件 -->
                <NavigationViewItem Content="准备文件" Tag="FilesPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8E5;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 参数面板 -->
                <NavigationViewItem Content="参数面板" Tag="ParametersPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE115;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 媒体信息 -->
                <NavigationViewItem Content="媒体信息" Tag="MediaInfoPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B2;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 混流 -->
                <NavigationViewItem Content="混流" Tag="MuxPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 合并 -->
                <NavigationViewItem Content="合并" Tag="ConcatPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE723;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 性能监控 -->
                <NavigationViewItem Content="性能监控" Tag="MonitorPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9D9;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 插件扩展 -->
                <NavigationViewItem Content="插件扩展" Tag="PluginsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xECAA;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!-- 软件设置 -->
                <NavigationViewItem Content="软件设置" Tag="SettingsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Frame x:Name="contentFrame"/>
        </NavigationView>
    </Grid>
</Window>
