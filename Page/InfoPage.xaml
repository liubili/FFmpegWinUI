<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FFmpegWinUI.Page.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FFmpegWinUI.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="24" MaxWidth="800">
            <!-- 应用图标和名称 -->
            <StackPanel HorizontalAlignment="Center" Spacing="16">
                <Border Width="120"
                        Height="120"
                        CornerRadius="12"
                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                        HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE8B2;"
                              FontSize="72"
                              Foreground="White"/>
                </Border>
                <TextBlock Text="FFmpeg WinUI"
                           Style="{StaticResource TitleTextBlockStyle}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="版本 1.0.0"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- 项目信息 -->
            <StackPanel Spacing="12">
                <TextBlock Text="关于此应用"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           FontWeight="SemiBold"/>
                <TextBlock TextWrapping="Wrap"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="FFmpeg WinUI 是从 VB.NET WinForms 项目完整移植到 WinUI 3 的现代化视频编码工具。"/>
                    <LineBreak/><LineBreak/>
                    <Run Text="本应用使用 FFmpeg 作为核心编码引擎，提供友好的图形界面和强大的参数配置功能。"/>
                </TextBlock>
            </StackPanel>

            <!-- 功能特性 -->
            <StackPanel Spacing="12">
                <TextBlock Text="主要功能"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           FontWeight="SemiBold"/>
                <StackPanel Spacing="4">
                    <TextBlock Text="• 批量视频转码和编码" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 完善的参数配置和预设管理" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 实时任务进度监控" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 音视频流混流和合并" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 性能监控和插件扩展" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="• 现代化 Fluent Design 界面" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <!-- 技术信息 -->
            <StackPanel Spacing="12">
                <TextBlock Text="技术栈"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           FontWeight="SemiBold"/>
                <StackPanel Spacing="4">
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="• 框架:"/>
                        <Run Text="WinUI 3 / .NET 8.0" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="• 编码引擎:"/>
                        <Run Text="FFmpeg" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="• 架构:"/>
                        <Run Text="MVVM (CommunityToolkit.Mvvm)" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <!-- 开发者信息 -->
            <StackPanel Spacing="12">
                <TextBlock Text="开发者"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           FontWeight="SemiBold"/>
                <TextBlock Text="原VB项目: 1059 Studio"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="WinUI移植: Claude Code"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- 链接 -->
            <StackPanel Spacing="8">
                <HyperlinkButton Content="FFmpeg 官方网站" NavigateUri="https://ffmpeg.org/"/>
                <HyperlinkButton Content="WinUI 3 文档" NavigateUri="https://learn.microsoft.com/windows/apps/winui/"/>
            </StackPanel>

            <!-- 版权信息 -->
            <TextBlock Text="© 2024 FFmpeg WinUI. All rights reserved."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
