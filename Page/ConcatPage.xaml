<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FFmpegWinUI.Page.ConcatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FFmpegWinUI.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="合并" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="将多个媒体文件合并为一个"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- 文件列表 -->
        <ListView Grid.Row="1"
                  x:Name="ConcatFileListView"
                  SelectionMode="Single"
                  CanReorderItems="True">
            <ListView.Header>
                <Grid Padding="12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="顺序" FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="1" Text="文件路径" FontWeight="SemiBold"/>
                </Grid>
            </ListView.Header>
        </ListView>

        <!-- 输出设置 -->
        <StackPanel Grid.Row="2" Spacing="8" Margin="0,16">
            <TextBlock Text="输出文件" FontWeight="SemiBold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" PlaceholderText="输出文件路径..."/>
                <Button Grid.Column="1" Content="浏览..." Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>

        <!-- 控制按钮栏 -->
        <CommandBar Grid.Row="3" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="添加文件"/>
            <AppBarButton Icon="Delete" Label="移除选中"/>
            <AppBarSeparator/>
            <AppBarButton Label="上移">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74A;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="下移">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE74B;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarSeparator/>
            <AppBarButton Icon="Accept" Label="开始合并" Style="{StaticResource AppBarButtonRevealStyle}"/>
        </CommandBar>
    </Grid>
</Page>
