<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FFmpegWinUI.Page.PluginsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FFmpegWinUI.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="插件扩展" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="管理和加载插件以扩展功能"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- 插件列表 -->
        <ListView Grid.Row="1" x:Name="PluginListView" SelectionMode="Single">
            <ListView.Header>
                <Grid Padding="12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="插件名称" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="版本" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="状态" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="操作" FontWeight="SemiBold"/>
                </Grid>
            </ListView.Header>
        </ListView>

        <!-- 控制按钮栏 -->
        <CommandBar Grid.Row="2" DefaultLabelPosition="Right" Margin="0,8,0,0">
            <AppBarButton Icon="Add" Label="加载插件"/>
            <AppBarButton Icon="Refresh" Label="刷新列表"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Setting" Label="插件设置"/>
        </CommandBar>
    </Grid>
</Page>
